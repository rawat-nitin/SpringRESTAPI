server:
  port: 8084

spring:
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: jdbc:h2:file:${user.home}/kotlin-tutorial/db
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto:


logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE


