{{> includes/header}}

<p><a href="/">Go home</a></p>

<h1>Update car</h1>

<form action="/car/update" method="post">
    <div>
        <label>Email: <br><input type="text" name="email" value="{{car.email}}"/> </label>
    </div>
    <div>
        <label>Password: <br><input type="password" name="password" /> </label>
    </div>
    <div>
        <label>Age: <br><input type="number" name="make" value="{{car.make}}"/> </label>
    </div>
    <input type="hidden" name="id" value="{{car.id}}"/>
    <div style="margin-top: 8px;">
        <input type="submit" value="Save"/>
    </div>
    <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
</form>

<p>{{message}}</p>

{{> includes/footer}}
